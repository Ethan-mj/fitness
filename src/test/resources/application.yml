spring:
  datasource:
    url: jdbc:h2:mem:fitness-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
app:
  seed-enabled: false
  jwt:
    secret: test-only-secret-with-more-than-32-characters
