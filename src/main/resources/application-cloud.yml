server:
  port: ${SERVER_PORT:80}
  address: 0.0.0.0

spring:
  main:
    lazy-initialization: true
  datasource:
    url: jdbc:mysql://${MYSQL_ADDRESS}/${MYSQL_DATABASE:fitness}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}
  jpa:
    database-platform: org.hibernate.dialect.MySQL57Dialect
    hibernate:
      ddl-auto: ${DB_DDL_AUTO:none}

app:
  seed-enabled: ${SEED_ENABLED:false}
  wechat:
    app-id: ${WECHAT_APP_ID:wx7eba904112ff9518}
  admin:
    initialize-enabled: ${ADMIN_INITIALIZE_ENABLED:true}
    initial-password: ${ADMIN_INITIAL_PASSWORD:${random.uuid}}
    wechat-openids: ${ADMIN_WECHAT_OPENIDS:}
    password-login-enabled: ${ADMIN_PASSWORD_LOGIN_ENABLED:false}
  cors:
    allowed-origin: ${CORS_ALLOWED_ORIGIN:*}
